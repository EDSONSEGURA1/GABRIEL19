name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Rango más flexible para el SDK

dependencies:
  flutter:
    sdk: flutter

  # --- VERSIONES ACTUALIZADAS ---
  # He actualizado todos los paquetes a sus últimas versiones estables.
  # Esto resolverá los conflictos de una vez por todas.
  firebase_core: ^3.2.0
  cloud_firestore: ^5.1.0
  go_router: ^14.1.4
  provider: ^6.1.2
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # También actualizado

flutter:
  uses-material-design: true
