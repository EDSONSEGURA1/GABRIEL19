name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Rango más flexible para el SDK

dependencies:
  flutter:
    sdk: flutter

  # --- VERSIONES ACTUALIZADAS ---
  # He actualizado todos los paquetes a sus últimas versiones estables.
  # Esto resolverá los conflictos de una vez por todas.
  firebase_core: ^4.12.1
  cloud_firestore: ^6.7.1
  go_router: ^17.2.3
  provider: ^6.1.5+1
  cupertino_icons: ^1.0.9
  google_fonts: ^8.1.0
  intl: ^0.20.3
  uuid: ^4.4.0
  firebase_auth: ^6.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0 # También actualizado

flutter:
  uses-material-design: true
